[package]
name = "llama_cpp_tests"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
llama_cpp = { workspace = true, features = ["native", "compat"] }
tokio = { workspace = true, features = ["full"] }
